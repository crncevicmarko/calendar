<Window x:Class="Calendar.View.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Calendar.Converters"
        xmlns:component="clr-namespace:Calendar.Component"
        mc:Ignorable="d"
        Title="Login" Height="400" Width="420"
        WindowStartupLocation="CenterScreen"
        Background="#F3F4F6">

    <Border CornerRadius="10" Background="White" Padding="30" Margin="20" >
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Welcome Back"
                       FontSize="24"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"/>

            <StackPanel Grid.Row="2">
                <TextBlock Text="User Name" Margin="0,0,0,3"/>
                <TextBox Text="{Binding LoginUserName}"
                         Name="userNameTextBox"
                         Padding="8"
                         Height="35"/>
            </StackPanel>

            <StackPanel Grid.Row="3" Margin="0,10,0,0">
                <TextBlock Text="Password" Margin="0,0,0,3"/>
                <component:BindablePasswordBox 
                    Password="{Binding LoginPassword, Mode=TwoWay}"
                    Height="35"/>
            </StackPanel>

            <Button Grid.Row="4"
                Content="Login"
                Command="{Binding LoginCommand}"
                HorizontalAlignment="Stretch"
                Height="40"
                Background="#3B82F6"
                Foreground="White"
                FontWeight="Bold"
                BorderThickness="0"
                Cursor="Hand"
                Margin="0,15,0,0"/>


            <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <TextBlock Text="Don't have an account?" Margin="0,0,5,0"/>
                <TextBlock Text="Register"
                           Foreground="#2563EB"
                           Cursor="Hand"
                           FontWeight="Bold"
                           TextDecorations="Underline"
                           MouseDown="btn_Register_Clicked_Down"/>
            </StackPanel>

        </Grid>
    </Border>
</Window>
