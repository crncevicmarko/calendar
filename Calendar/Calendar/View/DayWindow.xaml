<Window x:Class="Calendar.View.DayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Calendar.View"
        Title="DayWindow" Height="650" Width="600">

    <Window.Resources>
        <local:StartTimeToTopConverter x:Key="StartTimeToTopConverter"/>
        <local:DurationToHeightConverter x:Key="DurationToHeightConverter"/>
        <local:LaneToLeftConverter x:Key="LaneToLeftConverter"/>
        <local:LaneToWidthConverter x:Key="LaneToWidthConverter"/>
        <local:AbsenceTypeToBrushConverter x:Key="AbsenceTypeToBrushConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- All-day events -->
            <RowDefinition Height="*"/>
            <!-- Timeline -->
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <!-- Satovi -->
            <ColumnDefinition Width="*"/>
            <!-- Timeline -->
        </Grid.ColumnDefinitions>

        <!-- Satovi -->
        <ItemsControl Grid.Row="1" Grid.Column="0" ItemsSource="{Binding Hours}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" Height="50" VerticalAlignment="Top"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- All-day events (gore, iznad timeline) -->
        <ItemsControl Grid.Row="0" Grid.Column="1" ItemsSource="{Binding AllDayEvents}" Height="40">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="{Binding AbsenceType, Converter={StaticResource AbsenceTypeToBrushConverter}}"
                        Height="{Binding Converter={StaticResource DurationToHeightConverter}}"
                        CornerRadius="4" Padding="4">
                        <TextBlock Text="{Binding Title}" TextWrapping="Wrap"/>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Timeline sa Canvas za preklapajuće događaje -->
        <ScrollViewer Grid.Row="1" Grid.Column="1" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
            <Canvas x:Name="TimelineCanvas" Height="1200">
                <ItemsControl ItemsSource="{Binding TimedEvents}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <Canvas/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemContainerStyle>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="Canvas.Top" 
                                    Value="{Binding Start, Converter={StaticResource StartTimeToTopConverter}}"/>
                            <Setter Property="Canvas.Left">
                                <Setter.Value>
                                    <MultiBinding Converter="{StaticResource LaneToLeftConverter}">
                                        <Binding Path="LaneIndex"/>
                                        <Binding Path="LaneCount"/>
                                        <Binding ElementName="TimelineCanvas" Path="ActualWidth"/>
                                    </MultiBinding>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Width">
                                <Setter.Value>
                                    <MultiBinding Converter="{StaticResource LaneToWidthConverter}">
                                        <Binding Path="LaneIndex"/>
                                        <Binding Path="LaneCount"/>
                                        <Binding ElementName="TimelineCanvas" Path="ActualWidth"/>
                                    </MultiBinding>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ItemsControl.ItemContainerStyle>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="LightBlue"
                                    Height="{Binding Converter={StaticResource DurationToHeightConverter}}"
                                    CornerRadius="4" Padding="4">
                                <TextBlock Text="{Binding Title}" TextWrapping="Wrap"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Canvas>
        </ScrollViewer>

    </Grid>
</Window>
