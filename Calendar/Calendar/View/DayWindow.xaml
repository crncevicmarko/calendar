<Window x:Class="Calendar.View.DayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Calendar.Converters"
        Title="DayWindow" Height="650" Width="600">

    <Window.Resources>
        <local:StartTimeToTopConverter x:Key="StartTimeToTopConverter"/>
        <local:DurationToHeightConverter x:Key="DurationToHeightConverter"/>
        <local:LaneToLeftConverter x:Key="LaneToLeftConverter"/>
        <local:LaneToWidthConverter x:Key="LaneToWidthConverter"/>
        <local:AbsenceTypeToBrushConverter x:Key="AbsenceTypeToBrushConverter"/>

        <Style x:Key="HourLineStyle" TargetType="Rectangle">
            <Setter Property="Height" Value="1"/>
            <Setter Property="Fill" Value="#E0E0E0"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Margin" Value="0,49,0,0"/>
        </Style>

        <Style x:Key="HourTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#444"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,2,0,0"/>
        </Style>

        <Style x:Key="EventCardStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Background" Value="#B1D7FF"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.2"/>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- All-day events -->
        <ItemsControl Grid.Row="0" Grid.Column="1" ItemsSource="{Binding AllDayEvents}" Height="40">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="{Binding AbsenceType, Converter={StaticResource AbsenceTypeToBrushConverter}}"
                            CornerRadius="6" Padding="6" Margin="2">
                        <TextBlock Text="{Binding Title}"
                                   TextWrapping="Wrap"
                                   FontWeight="Bold"
                                   Foreground="Black"/>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Scroll for both hours + timeline -->
        <ScrollViewer Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                      HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">

            <Grid Background="WhiteSmoke">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <ItemsControl Grid.Column="0" ItemsSource="{Binding Hours}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Height="50">
                                <TextBlock Text="{Binding}" Style="{StaticResource HourTextStyle}"/>
                                <Rectangle Style="{StaticResource HourLineStyle}"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- TIMELINE -->
                <Canvas x:Name="TimelineCanvas" Grid.Column="1" Height="1200" Background="White">

                    <ItemsControl ItemsSource="{Binding TimedEvents}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">

                                <Setter Property="Canvas.Top"
                                        Value="{Binding Start, Converter={StaticResource StartTimeToTopConverter}}"/>

                                <Setter Property="Canvas.Left">
                                    <Setter.Value>
                                        <MultiBinding Converter="{StaticResource LaneToLeftConverter}">
                                            <Binding Path="LaneIndex"/>
                                            <Binding Path="LaneCount"/>
                                            <Binding ElementName="TimelineCanvas" Path="ActualWidth"/>
                                        </MultiBinding>
                                    </Setter.Value>
                                </Setter>

                                <Setter Property="Width">
                                    <Setter.Value>
                                        <MultiBinding Converter="{StaticResource LaneToWidthConverter}">
                                            <Binding Path="LaneIndex"/>
                                            <Binding Path="LaneCount"/>
                                            <Binding ElementName="TimelineCanvas" Path="ActualWidth"/>
                                        </MultiBinding>
                                    </Setter.Value>
                                </Setter>

                            </Style>
                        </ItemsControl.ItemContainerStyle>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource EventCardStyle}"
                                        Height="{Binding Converter={StaticResource DurationToHeightConverter}}">
                                    <TextBlock Text="{Binding Title}" TextWrapping="Wrap" Foreground="#0A2A43"/>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>

                    </ItemsControl>

                </Canvas>
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>
