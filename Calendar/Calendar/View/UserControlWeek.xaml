<UserControl x:Class="Calendar.View.UserControlWeek"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converter="clr-namespace:Calendar.Converters"
             mc:Ignorable="d"
             MinHeight="150" MinWidth="700">

    <UserControl.Resources>
        <converter:AbsenceTypeToBrushConverter x:Key="AbsenceTypeToBrushConverter"/>
    </UserControl.Resources>

    <Border Background="#F3F4F6" CornerRadius="10" Padding="10">
        <ItemsControl ItemsSource="{Binding DaysOfWeek}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="7" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="White" BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="5" Margin="5" Padding="5">
                        <StackPanel>
                            <TextBlock Text="{Binding Date, StringFormat={}{0:dd}}" 
                               FontWeight="Bold" FontSize="16" 
                               HorizontalAlignment="Center" Margin="0,0,0,5"/>

                            <ItemsControl ItemsSource="{Binding Absences}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{Binding AbsenceType, Converter={StaticResource AbsenceTypeToBrushConverter}}"
                                        CornerRadius="4" Margin="2" Padding="2">
                                            <TextBlock Text="{Binding Title}" FontSize="12" Foreground="Black"/>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <ItemsControl ItemsSource="{Binding Appointments}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#BBDEFB" CornerRadius="3" Padding="3" Margin="0,0,0,2">
                                            <TextBlock Text="{Binding Title}" FontSize="12" TextWrapping="Wrap"/>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

    </Border>
</UserControl>